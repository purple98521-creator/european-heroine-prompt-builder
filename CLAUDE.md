# ヒロインプロンプトビルダー（複数シリーズ）仕様書

歴史上の女帝・王妃・ヒロインを対象にした、AI画像生成用プロンプトを組み立てるWebアプリ群。
参考にした元サイト：「源氏物語 ヒロインプロンプトビルダー」（https://aquamarine-meringue-aa9ff1.netlify.app/）と同じ仕組みを、西洋・イギリス・中国のヒロインに展開したもの。

## 公開先

- GitHubリポジトリ: `git@github.com:purple98521-creator/european-heroine-prompt-builder.git`
- Netlify（GitHub連携、mainブランチにpushすると自動デプロイ）: https://heroine-prompt-builder.netlify.app/
- git config `user.name` / `user.email` は既にこのマシンでグローバル設定済み

## ページ構成（シリーズ）

各シリーズは**完全に独立した単一HTMLファイル**（インラインCSS・インラインJS、ビルド不要、外部依存なし）。

| ファイル | シリーズ | 配色 | 対象ヒロイン（5人×3場面＝15パターン） |
|---|---|---|---|
| `index.html` | ヨーロッパ編 | マゼンダ×シャンパンゴールド | マリー・アントワネット／マリア・テレジア／エカチェリーナ2世／ジョゼフィーヌ／エリザベート（シシィ） |
| `england.html` | イギリス編 | ロイヤルブルー×シャンパンゴールド | アン・ブーリン／エリザベス1世／メアリー・ステュアート／ヴィクトリア女王／ナイチンゲール |
| `china.html` | 中国編 | 翡翠グリーン×シャンパンゴールド | 虞美人／王昭君／則天武后／楊貴妃／西太后 |
| `genji.html` | 源氏物語編 | 墨・紫・朱・金（平安調） | 藤壺／紫の上／葵の上／六条御息所／明石の上／朧月夜／玉鬘／花散里／女三宮／空蝉／夕顔／浮舟（15人） |
| `fashion.html` | ファッショナブル編（汎用） | パープル系 | 歴史上の人物ではなく、スタイル・被写体・服装・配色・背景・モチーフの6カテゴリから自由に組み合わせる汎用プロンプトビルダー |

`genji.html`と`fashion.html`は元々ユーザーが別のNetlifyサイトとして個別に作っていたものを、このリポジトリに統合したもの（ブラウザで表示されたDOMを書き起こして移植）。中身のエンジン設計は他の3ページとは別物（`genji.html`は身分/装束/色目/文様の依存関係が複雑、`fashion.html`はJSでチップを動的生成する構造）なので、他ページと同じ`HEROINES`/`COSTUMES`方式には統一されていない。**この2ページを修正するときは、それぞれのファイル内のロジックを個別に読むこと。**

各ページの`<header>`に他シリーズへのリンクを相互に張っている。新しいシリーズを追加したら、既存ページ全部のリンクにも追加すること。

未着手（元の13人構成の残り）：サリバン先生、クレオパトラ7世、ジャンヌ・ダルク。
※サリバン先生は「装束の格」という枠組みが馴染まないため、同フォーマットで作る場合は要検討。

## 技術構成の共通ルール

- 単一HTMLファイル、フレームワーク不使用（vanilla JS）。理由：ビルド不要でNetlifyにそのまま置ける、依存関係ゼロ。
- 3ファイルとも**ほぼ同一のエンジン（JSロジック）**を持つ。新シリーズを作るときは既存ファイルをコピーしてデータ部分だけ差し替えるのが最速。
- 見出しフォント: `"HGエレガント平成明朝体W3"`（無ければ明朝体にフォールバック）。本文: ゴシック体。
- 配色はCSS変数（`--bg` `--ink` `--gold` `--magenta` 等）にまとめてあるので、シリーズごとの色替えは`:root`の値を差し替えるだけでよい（変数名は`--magenta`のままでも実際の色は青や緑にしてよい＝命名と実際の色が一致していなくても問題ない）。

### データ構造（各ファイル共通のJS配列）

- `HEROINES`: ヒロイン本体。1人につき3variant（場面違い）。`group`でCOSTUME/PATTERN/MOTIFのフィルタに使う。`defCostume` `defPalette` `defExpr` `defMotifs`がヒロイン切替時の初期値。
- `COSTUMES` / `PATTERNS` / `MOTIFS`: `group`フィールドで対象ヒロインに絞り込み。`group:'both'`は全ヒロイン共通で表示。
- `PALETTES`: 絞り込みはせず全員に表示、`star`フィールドでゆかりの深い色に★マークをつけるのみ（見た目のヒントで機能制限ではない）。
- `SCENES` `STYLES` `DEFORM` `TONE` `DYN` `FRAMING` `ANGLE` `HANDS` `EXPR` `FACE_REF` `MAKEUP` `FEATURES` `PROPS` `BOLD` `DECOR` `LIGHT` `TEXTSTYLE` `SIZES`: 全ヒロイン共通の選択肢群。
- 各要素オブジェクトの`def:true`は「◎」表示（推奨マーク、視覚的ラベルのみで実際のデフォルト値には連動しない＝実際の初期値は`state`オブジェクトとボタン処理内で別途指定）。
- `fav:true`は「♥」表示（作成者おすすめマーク）。現状は画風の「グラフィカルな広告ポスター風」に付与。

### プロンプト組み立てロジック

`buildPromptFor(heroine, overrides)`が全選択項目を順番に連結して1つの英語プロンプト文字列を作る（`buildPromptFor`内の並び順がそのまま出力の語順になる）。「15人分をまとめて書き出す」ボタンは、各ヒロインについて衣装・色目・モチーフ・表情だけをそのヒロインの既定値に差し替えて一括出力する（`patterns`は現在選択中のものを、そのヒロインの`group`に該当するものだけフィルタして使う＝グループをまたいだ文様の混入を防ぐため）。

## これまでに直した/学んだ重要な注意点（再発防止用）

1. **バッチ書き出しの文様混入バグ**：「15人分まとめて書き出す」で、現在選択中の`patterns`をそのまま全ヒロインに使うと、他グループ専用の文様が紛れ込む。必ず対象ヒロインの`group`でフィルタしてから使うこと（3ファイルとも対応済み）。
2. **画風どうしの矛盾**：「グラフィカルな広告ポスター風」に「顔は写真のまま保つ」という指示を埋め込んでいた結果、「デフォルメ：しっかり（アニメ寄り）」と組み合わせると指示が競合し、顔だけ実写に引っ張られた。→ 画風側から顔の写実/非写実を指定する文言は入れず、レイアウト（色面ブロック・コラージュ紙・スパークル・タイポグラフィ）の話だけに専念させる設計にした。**新しい画風を追加するときも、顔のレンダリング方法を強制する文言は入れないこと**（それはデフォルメ/画風の別選択肢や参照画像設定の役目）。
3. **写真っぽくなりすぎる問題**：画風の説明文には明示的に"not a photograph"等を入れて、写実に流れないよう強めに書く。特に参照写真を使う設定と組み合わさると写実に寄りやすいので、イラスト系の画風文言は強く・具体的に書く。
4. **顔の角度が参照写真のままコピーされる問題**：「本人に似せたイラストにする」の指示に、「頭の角度・視線・ポーズは参照写真をコピーせず自由に変えてよい」という一文を明記して解決。
5. **アニメ寄り×参照画像のバランス**：デフォルメ「しっかり（アニメ寄り）」＋参照画像「本人に似せたイラストにする」の組み合わせが、リアルになりすぎず適度に似る、とユーザーから好評。UIの説明文にもこのコツを明記済み。

## ローカルでの動作確認方法

このリポジトリはブラウザのJSがそのまま動く静的サイトなので、`file://`で直接開くとJSが動かないことがある（サンドボックス環境の場合）。ローカルサーバーを立てて確認する：

- `.claude/launch.json` に `static-preview` という設定があり、`.claude/static-server.ps1`（PowerShellで書いた簡易HTTPサーバー、ポート8532）を起動する。
- `.claude/`ディレクトリは`.gitignore`でリポジトリから除外しているので、リポジトリをclone/pullしただけでは付いてこない。新しい環境（自宅PCなど）で動作確認したい場合は、この仕様書の内容を参考に同等のローカルサーバー設定を作るか、他の方法（`python -m http.server`等、実際に動くPython/Node.js等がインストールされていれば）で代用する。

## デプロイ手順（このPCで確立済み）

```bash
git add -A
git commit -m "変更内容"
git push
```

- Windows環境で、ユーザー名に日本語（非ASCII文字）が含まれる場合、Git for Windows同梱のsshクライアントだとGitHubへの接続で `Host key verification failed` になることがある。対策として `git config --global core.sshCommand "C:/WINDOWS/System32/OpenSSH/ssh.exe"` を設定済み（システムのOpenSSHを強制使用）。自宅PCでも同じ症状が出たら同様に設定する。
- pushすればNetlifyが自動で再デプロイする（手動操作不要）。反映まで1分程度。

## 自宅PCなど新しい環境で作業を始めるときの手順

1. GitHubアカウントに自宅PC用のSSH鍵を登録済みであることを確認（未登録なら `ssh-keygen` で鍵を作り、公開鍵をGitHubの Settings > SSH and GPG keys に登録）
2. `git clone git@github.com:purple98521-creator/european-heroine-prompt-builder.git`
3. Claude Codeをこのフォルダで開く（このCLAUDE.mdが自動で読み込まれる）
4. 「GitHubの最新を取り込んで」と伝える（`git pull`）
5. あとは通常通り、日本語で要望を伝えるだけでよい
